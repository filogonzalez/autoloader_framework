# --- Secrets (NEVER commit) ---
.env
*.env
*.key
*.pem
secrets.json
.databrickscfg

# --- Databricks / DABs (machine/workspace-local) ---
.databricks/
__pycache__/
*.pyc

# --- Python / venv ---
.venv/
.python-version

# --- Editor / OS ---
.DS_Store
.vscode/*.log

# --- Polly orchestration scratch (local; not repo context) ---
.polly/
.worktrees/

# NOTE: .harness/, AGENTS.md, CLAUDE.md are now TRACKED on purpose
# (so sub-agent worktrees see the repo's operating rules + verifier).
